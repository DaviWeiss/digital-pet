<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/stylesheets/global.css" rel="stylesheet" type="text/css">
    <link href="/stylesheets/register.css" rel="stylesheet" type="text/css">
    <title>Cadastro | Digital Pet</title>
</head>
<body>
    <header>
        <section class="header-cadastro">
            <a href="/">
                <img src="/images/header/logo-desktop.svg" alt="Logo digital pet">
            </a>
        </section>
    </header>

    <main>
        <div class="box-form">
            <form method="POST" class="column" action="/usuario/criar">
                <label for="email">E-mail*</label>
                <input value="<%= locals.oldData ? oldData.email : null%>" type="text" name="email" id="email" placeholder="Digite seu e-mail">
                <span id="errors-email" class="error"></span>
                <% if(locals.errors && errors.email) { %>
                    <span class="error" id="error-email"><%= errors.email.msg %></span>
                <% } %>    

                <label for="name">Nome*</label>
                <input value="<%= locals.oldData ? oldData.name : null%>"  type="text" name="name" id="name" placeholder="Digite seu nome">
                <span id="errors-name" class="error"></span>
                <% if(locals.errors && errors.name) { %>
                    <span class="error"><%= errors.name.msg %></span>
                <% } %>    

                <label for="lastName">Sobrenome*</label>
                <input value="<%= locals.oldData ? oldData.lastName : null%>"  type="text" name="lastName" id="lastName" placeholder="Digite seu sobrenome">
                <span id="errors-lastName" class="error"></span>
                <% if(locals.errors && errors.lastName) { %>
                    <span class="error"><%= errors.lastName.msg %></span>
                <% } %>

                <label for="CPF">CPF*</label>
                <input type="CPF" name="CPF" id="CPF" placeholder="Digite seu CPF">
                <span id="errors-CPF" class="error"></span>
                <% if(locals.errors && errors.CPF) { %>
                    <span class="error"><%= errors.CPF.msg %></span>
                <% } %>

                <label for="celular">Celular*</label>
                <input value="<%= locals.oldData ? oldData.celular : null%>" type="tel" name="celular" id="celular" placeholder="Digite seu celular">
                <span id="errors-celular" class="error"></span>
                <% if(locals.errors && errors.celular) { %>
                    <span class="error"><%= errors.celular.msg %></span>
                <% } %>

                <label for="password">Crie uma senha*</label>
                <input value="<%= locals.oldData ? oldData.password : null%>" type="password" name="password" id="password" placeholder="Crie sua senha">
                <div id="errors-password">
                    <span id="erros-password-span">A senha deve conter:</span>
                    <ul>
                        <li>8 caracteres no mínimo;</li>
                        <li>1 Letra Maiúscula no mínimo;</li>
                        <li>1 Número no mínimo;</li>
                        <li>1 Símbolo no mínimo: $*&@#"</li>
                    </ul>
                </div>
                <% if(locals.errors && errors.password) { %>
                    <span class="error"><%= errors.password.msg %></span>
                <% } %>    

                <label for="confirmPassword">Confirme a sua senha*</label>
                <input value="<%= locals.oldData ? oldData.confirmPassword : null%>" type="password" name="confirmPassword" id="confirmPassword" placeholder="Confirme a sua senha">
                <span id="errors-confirmPassword" class="error"></span>
                <% if(locals.errors && errors.confirmPassword) { %>
                    <span class="error"><%= errors.confirmPassword.msg %></span>
                <% } %>

                <button type="submit">Criar conta</button>
                <button type="button" class="btn-go-back">Voltar</button>
                
                <p class="info">
                    Ao criar uma conta, você concorda com os Termos e Condições de Uso da Petlove. Para saber como tratamos os seus dados pessoais, acesse o nosso Aviso de Privacidade.
                </p>
            </form>
        </div>
    </main>

    <script src="/javascripts/global.js"></script>
    <script src="/javascripts/register.js"></script>
    
</body>
</html>